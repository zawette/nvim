" gs to get git status
nnoremap <leader>gs :G<CR>
" g left arrowkey to accept left side during merge 
nnoremap <leader>g<left> :diffget //2<CR>
" g right arrowkey to accept right side during merge ( the side we merge from ) 
nnoremap <leader>g<right> :diffget //3<CR>
